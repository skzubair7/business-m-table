# business-m-table
a web-based business table application enabling vendors to add products with prices, calculate total sales, and track product-wise details dynamically.  Implemented CRUD operations with support for creating new tables, editing and deleting entries, and maintaining table history with resume functionality.
